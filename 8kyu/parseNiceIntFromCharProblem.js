/**********************
  Challenge: To return the girls age as an integer from the string statement;
  
  P - a string
  R - return
  E - ("4 years old"), 4;
  P - take out the value at index zero of the string and convert it to an integer;
      return the result after conversion;
**********************/

function getAge(inputString){
    // return the girl's correct age as an integer. Happy coding :) 
    //   return Number(inputString[0]);
      return parseInt(inputString[0]);
}