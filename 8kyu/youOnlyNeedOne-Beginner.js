// Challenge: To check if a particular element exist within the given array
// The includes method could be used to solve this challenge

function check(a, x) {
    return a.includes(x);
}
  
// Alternative: usng a function expression and an arrow function
// const check = (a, x) => a.includes(x)