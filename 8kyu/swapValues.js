/*****************************
  Challenge: To pass an array into a function and swap the values
  
  P - an array
  R - return a reversed or swapped version of the array;
  E - arr[0], 2;
      arr[1], 1;
  P - call the reverse method on the array and that should solve the problem
*****************************/

function swapValues(arr) {
    arr = arr.reverse();
}