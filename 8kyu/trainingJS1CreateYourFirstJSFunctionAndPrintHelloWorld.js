/****************
  Challlenge: to create a function that logs hello world to the console
  
  no parameter,
  log 'Hello World!' to the console,
  calling the helloWorld(), should log the right message,
  
  use an arrow function to create a function whose output is logged to the console
  
****************/
// Following all set instructions;

// function helloWorld(){
//   let str = 'Hello World!';
//   console.log(str);
// }

const helloWorld = _ => {
    let str = 'Hello World!';
    console.log(str);
}