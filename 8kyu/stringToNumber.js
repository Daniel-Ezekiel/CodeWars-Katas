// Challenge: to return a number string as a number
// This can be done by using the Number method and passing the string as the parameter

var stringToNumber = function(str){
    return Number(str);
}
  
// Alternative using a function expression and an arrow function
// const stringToNumber = str => Number(str)