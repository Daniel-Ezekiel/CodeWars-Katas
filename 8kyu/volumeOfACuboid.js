/*******************
  PREP
  Challenge: To return the volume of a cuboid;
  P - three numbers as params;
  R - return, not console.log(), the volume
  E - (1,2,2), 4;
      (6.3,2,5), 63;
  P - multiply the three param values;
      returns the product as the result;
*******************/

class Kata {
    static getVolumeOfCuboid(length, width, height) {
      // your code here
      return length * width * height;  
    }
}