/*******************
  PREP
  Challenge: To multiply two numbers;
  P - two numbers;
  R - return, not console.log(), a product
  E - (2, 3), 6;
      (2, 8), 16;
      (8, 3), 24;
  P - create a function,
      takes two params,
      within the code block, multiply param1 with param2,
      return the product;
*******************/

// Write here your multiply-function
const multiply = (n1, n2) => n1*n2;