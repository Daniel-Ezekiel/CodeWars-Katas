/**************
  PREP
  Challenge: To convert each character within a string into uppercase
  
  P -  string
  R - return, not console.log(), the string in uppercase
  E - str = hello, return HELLO,
      str = danno, return DANNO,
      str = fAssH, return FASSH.
  
  
  P - the .toUpperCas() method is the best method to call on the string to solve this problem
**************/

function makeUpperCase(str) {
  return str.toUpperCase();
}