// Challenge: To join all the strings in an array into a sentence(Sentence Smash)
// This will be acheived using the join method with the argument of a space within the strings like so ' ';

function smash (words) {
    return words.join(' ')
};
 
// Alternative: using a function expression and an arrow function
// const smash = words => words.join(' ');